const sceneFetch = (socket, args) => {
  //   const { action, result, payload } = args;
  //   socket.emit("action:result", {
  //     action: action,
  //     result: result,
  //     payload: payload,
  //   });
};

const sceneCache = (socket, args) => {
  //   const { action, result, payload } = args;
  //   socket.emit("action:result", {
  //     action: action,
  //     result: result,
  //     payload: payload,
  //   });
};

const cachedSceneUpdate = (socket, args) => {
  //   const { action, result, payload } = args;
  //   socket.emit("action:result", {
  //     action: action,
  //     result: result,
  //     payload: payload,
  //   });
};

const cachedSceneUpsert = (socket, args) => {
  //   const { action, result, payload } = args;
  //   socket.emit("action:result", {
  //     action: action,
  //     result: result,
  //     payload: payload,
  //   });
};

export { sceneFetch, sceneCache, cachedSceneUpdate, cachedSceneUpsert };
